export const PLATFORM_FEE_RATE = 0.15;
export const PLATFORM_FEE_RATE_PREMIUM = 0.10;
export const VAT_RATE = 0.07;

export const PLATFORM_FEE_PCT = Math.round(PLATFORM_FEE_RATE * 100);          // 15
export const PLATFORM_FEE_PCT_PREMIUM = Math.round(PLATFORM_FEE_RATE_PREMIUM * 100); // 10
export const VAT_PCT = Math.round(VAT_RATE * 100);                             // 7
